/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // if deploying to `https://yourusername.github.io/repo-name/`
    basePath: '/catalog/tailwind/keynote',
    assetPrefix: '/catalog/tailwind/keynote',
}

module.exports = nextConfig
